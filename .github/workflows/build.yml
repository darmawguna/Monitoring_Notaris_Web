name: Build & Push Image (Docker Hub)

on:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
       # --- TAMBAHKAN LANGKAH DEBUGGING INI ---
      - name: List files in the checkout directory
        run: ls -la

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push production image
        uses: docker/build-push-action@v6
        with:
          context: .
          # Menggunakan Dockerfile multi-stage untuk build
          file: ./Dockerfile
          push: true
          tags: |
            darmawiguna/docuhub:latest
            darmawiguna/docuhub:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max